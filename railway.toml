[build]
builder = "DOCKERFILE"
dockerfilePath = "api/Dockerfile"

[deploy]
preDeployCommand = "sh -c 'cd /app && alembic upgrade head'"
startCommand = "gunicorn finance_api.main:app -c gunicorn.conf.py"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
