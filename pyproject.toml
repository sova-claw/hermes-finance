[tool.pytest.ini_options]
pythonpath = ["api"]
asyncio_mode = "auto"
env_files = [".env.test"]

[tool.mypy]
strict = true
python_version = "3.12"

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "RUF"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]
